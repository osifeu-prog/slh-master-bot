services:
  bot-local:
    build: .
    volumes:
      - .:/app
    environment:
      - TELEGRAM_TOKEN=${LOCAL_BOT_TOKEN}
      - REDIS_URL=redis://redis-local:6379
    depends_on:
      - redis-local

  redis-local:
    image: redis:7-alpine
    ports:
      - "6380:6379"
